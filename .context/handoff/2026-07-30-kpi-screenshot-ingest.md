# 引き継ぎ: KPI振り返り → screenshot-ingest 基盤構築（2026-07-30）

リモートセッション（ブランチ `claude/kpi-review-a82uvj`）からの引き継ぎ書。
次のセッション（Mac ローカル等）はこのファイルを読めば続きから作業できる。

## 経緯

1. KPI 振り返りを実施。やり切り率が 100%(8/8) → 47%(41/88) に低下したが、
   分母急増・計測タイミングのブレ（火曜手動/土曜実行）・スキップ除外未実装が
   原因で週次比較が不能。KPI2（集中時間）・KPI3（振り返り実施率）は計測未設計。
   詳細は Notion「週次レビュー｜2026-07-18週」「週次KPIログ（やり切り率）」参照。
2. スマホ専用データ（Web 版がないアプリ）の取り込み経路としてスクショ→OCR
   （経路 B）を採用し、`screenshot-ingest` スキルを構築した（ADR 0020）。

## 完了済み

- `.claude/skills/screenshot-ingest.md` / `automation/screenshots/*.yaml` /
  ADR 0020 / AGENTS.md 更新（コミット b8deb6c、プッシュ済み）
- Slack プライベートチャンネル **#kpi-screenshot-inbox**（ID: `C0BLUJVH2GZ`）を
  作成し、使い方の案内を投稿済み

## 残タスク（優先順）

1. **銀行アダプタの確定**: `automation/screenshots/bank-balance.yaml` の
   アプリ名・metrics をユーザーに確認して埋め、`enabled: true` にする
2. **本ブランチを main にマージ**（PR 未作成。ユーザーの指示があれば作成）
3. **Routine 登録（マージ後）**: スキル末尾の「週次スケジュール登録」どおり
   リマインダー（JST 土 21:00）と取り込み（JST 日 06:30）の 2 本を登録する
4. **スマホ直接接続（ユーザー希望）**: リモートセッションからは LAN 不達で
   不可能だった。Mac ローカルセッションで `adb pair` を実施する場合、
   ペアリングコードは失効している可能性が高いので再発行してもらうこと
5. **クラウド完結タスク（別スキル化の方針、今回スコープ外）**:
   Notion への振り返り記録 / 週次レビュー Routine の計測仕様固定
   （日曜 7:00 統一・対象期間 月〜日・スキップ除外）/ OYKOT kr_progress 入力 /
   カレンダー二重予定の発生元特定

## 参照

- KPI 定義: Notion「月次レビュー - KPI追跡」（やり切り率 80% / 週5h集中 90% /
  振り返り実施率 100%）
- KPI ログ: Notion「週次KPIログ（やり切り率）」（週次レビュー routine が自動追記）
