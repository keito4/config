# Environment Variables Template
# Copy this file to .env and set your actual values
# DO NOT commit .env files with real credentials to version control

# Git Configuration
# Set these environment variables or use 'git config --global' commands
GIT_AUTHOR_NAME="Your Name"
GIT_AUTHOR_EMAIL="your.email@example.com"
GIT_COMMITTER_NAME="Your Name"
GIT_COMMITTER_EMAIL="your.email@example.com"

# Code Server Configuration
# Set a strong password for code-server access
CODE_SERVER_PASSWORD="your-secure-password-here"

# SSH Configuration
# If using SSH keys, specify the path to your key files
# SSH_PRIVATE_KEY_PATH="~/.ssh/id_rsa"
# SSH_PUBLIC_KEY_PATH="~/.ssh/id_rsa.pub"

# Example secure password generation:
# openssl rand -base64 32