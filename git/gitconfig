[user]
	# Configure these with environment variables or through git config commands:
	# git config --global user.name "Your Name"
	# git config --global user.email "your.email@example.com"
	# git config --global user.signingkey "your-ssh-public-key"
	#
	# For security, personal information should not be hardcoded in this file.
	# Instead, use:
	# export GIT_AUTHOR_NAME="Your Name"
	# export GIT_AUTHOR_EMAIL="your.email@example.com"
	# export GIT_COMMITTER_NAME="Your Name"
	# export GIT_COMMITTER_EMAIL="your.email@example.com"

[ghq]
	root = ~/develop

[core]
	excludesfile = ~/.gitignore
	attributesfile = ~/.gitattributes
	editor = emacs -nw

[gpg]
	format = ssh

[commit]
	gpgsign = true

# Include common configuration
[include]
	path = ~/.gitconfig.d/common.conf

# Include platform-specific configuration
# Users should create a symlink: ~/.gitconfig.d/platform.conf -> macos.conf or linux.conf
[include]
	path = ~/.gitconfig.d/platform.conf