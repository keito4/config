{
  "$schema": "https://unpkg.com/@anthropic-ai/claude-code/dist/mcp.schema.json",
  "mcpServers": {
    "linear": {
      "command": "npx",
      "args": ["-y", "@mseep/linear-mcp"],
      "env": {
        "LINEAR_API_KEY": "${LINEAR_API_KEY}"
      }
    }
  }
}
