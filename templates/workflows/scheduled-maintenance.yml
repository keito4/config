name: Scheduled Maintenance

on:
  schedule:
    # Every Monday 06:00 UTC (JST 15:00)
    - cron: '0 6 * * 1'
  workflow_dispatch:
    inputs:
      mode:
        description: 'Maintenance mode'
        required: false
        default: 'full'
        type: choice
        options:
          - full
          - quick
          - check-only

permissions: {}

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false

jobs:
  maintenance:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: write
      pull-requests: write
      issues: write
      id-token: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          fetch-depth: 1

      - name: Run scheduled maintenance
        id: maintenance
        uses: anthropics/claude-code-action@b47fd721da662d48c5680e154ad16a73ed74d2e0 # v1
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          github_token: ${{ github.token }}
          prompt: |
            /repo-maintenance --mode ${{ inputs.mode || 'full' }}

            After completing maintenance, summarize:
            - What was checked
            - What was updated
            - What needs manual attention

            If changes were committed and pushed, do NOT create a pull request yourself.
            This workflow creates the pull request from the Claude branch after Claude finishes.

            If no updates are needed, do NOT create an issue.
          claude_args: |
            --allowedTools "Bash(npm:*),Bash(pnpm:*),Bash(npx:*),Bash(node:*),Bash(git:*),Bash(curl:*),Bash(jq:*)"

      - name: Create maintenance pull request
        if: steps.maintenance.outputs.branch_name != ''
        env:
          GH_TOKEN: ${{ steps.maintenance.outputs.github_token || github.token }}
          CLAUDE_BRANCH: ${{ steps.maintenance.outputs.branch_name }}
          DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}
          MODE: ${{ inputs.mode || 'full' }}
        run: |
          if [ -z "$CLAUDE_BRANCH" ]; then
            echo "Claude did not create a branch. Skipping PR creation."
            exit 0
          fi

          EXISTING_PR=$(gh pr list --head "$CLAUDE_BRANCH" --state open --json number --jq '.[0].number // empty')
          if [ -n "$EXISTING_PR" ]; then
            echo "PR #$EXISTING_PR already exists for $CLAUDE_BRANCH."
            exit 0
          fi

          BODY=$(printf "Automated scheduled maintenance run.\n\nMode: %s\nBranch: \`%s\`" "$MODE" "$CLAUDE_BRANCH")

          gh pr create \
            --head "$CLAUDE_BRANCH" \
            --base "$DEFAULT_BRANCH" \
            --title "chore: scheduled maintenance" \
            --body "$BODY"

      - name: Post failure issue
        if: failure()
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          EXISTING=$(gh issue list --label "maintenance" --state open --json number --jq 'length')
          if [ "$EXISTING" -gt 0 ]; then
            echo "Open maintenance issue already exists. Skipping."
            exit 0
          fi

          gh issue create \
            --title "fix: Scheduled Maintenance failed" \
            --label "maintenance,bug" \
            --body "## Scheduled Maintenance Failure

          The scheduled maintenance workflow failed.

          **Run:** ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          **Mode:** ${{ inputs.mode || 'full' }}

          Please check the workflow logs and re-run manually if needed.

          ---
          *Auto-generated by [scheduled-maintenance.yml](.github/workflows/scheduled-maintenance.yml)*"
